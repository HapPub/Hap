# HapCLI Examples

Examples should stay public-safe and synthetic.

Do not copy private project config directly into this directory.

The first example family is the synthetic ExampleApp-style `cjpm.toml` profile fixture.
The fixture name is historical and synthetic; it must not contain real private project paths, credentials, providers, or repository locations.
